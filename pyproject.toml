[project]
name = "web-personal-blog"
version = "0.1.0"
description = "Server-rendered markdown blog (Cloudflare Python Worker)"
requires-python = ">=3.12"
dependencies = ["fastapi", "jinja2", "mistune"]

[dependency-groups]
dev = ["workers-py", "workers-runtime-sdk", "pytest"]
